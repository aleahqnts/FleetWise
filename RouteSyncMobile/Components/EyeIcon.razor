@* The icon inside a reveal button, reporting the state of the field rather than the
   action on offer: a slashed eye while the password is hidden, an open one while it is
   readable. Kept in one place so every password field on every page agrees. *@

@if (Shown)
{
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M2 12s3.5-7 10-7 10 7 10 7-3.5 7-10 7-10-7-10-7z" /><circle cx="12" cy="12" r="3" /></svg>
}
else
{
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M2 12s3.5-7 10-7c2 0 3.8.6 5.3 1.5M22 12s-3.5 7-10 7c-2 0-3.8-.6-5.3-1.5" /><path d="M3 3l18 18" /><circle cx="12" cy="12" r="3" /></svg>
}

@code {
    /// <summary>Whether the field it belongs to is currently readable.</summary>
    [Parameter] public bool Shown { get; set; }
}
